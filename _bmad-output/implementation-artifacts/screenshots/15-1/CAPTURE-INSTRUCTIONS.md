# Story 15-1 — Captures requises (DoD Epic 15)

Pour valider la story, enregistrer 2 captures dans ce dossier :

1. **15-1-login.png** — Page `/login` (bandeau vert, logo, pas de nav)
2. **15-1-caisse.png** — Page `/caisse` ou `/admin` connectée (bandeau vert + nav + dropdown)

Procédure : `npm run dev` → ouvrir http://localhost:4173 (ou 4175) → capturer (Win+Shift+S ou outil screenshot).

Une fois les fichiers ajoutés, relancer la code review (run-story 15-1).
