<!-- deprecated: use system.md -->

# Deprecated alias

This file is a legacy alias. Workers and parent **must resolve** to [`system.md`](system.md).

For `kind: system` or legacy `type: arch`, load `domains/system.md`.
