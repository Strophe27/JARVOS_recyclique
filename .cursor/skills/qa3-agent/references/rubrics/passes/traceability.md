# Pass — Traceability

Map requirements to outputs. Verify each requirement has a traceable artifact or test.

Check:
- Requirements without implementation
- Implementation without requirement
- Orphaned acceptance criteria

Requires `requirements_exist: true` AND `expected_outputs_exist: true` for dedicated pass.
