# Staging Environment Configuration
# Copy this file to .env.staging and fill in the actual values

# Database
POSTGRES_USER=recyclic
POSTGRES_DB=recyclic
POSTGRES_PORT=5432
POSTGRES_HOST=postgres
POSTGRES_PASSWORD=<STAGING_SECURE_PASSWORD>

# Security
SECRET_KEY=<LONG_RANDOM_SECRET_KEY_FOR_JWT>

# Telegram Bot
TELEGRAM_BOT_TOKEN=<STAGING_BOT_TOKEN>
ADMIN_TELEGRAM_IDS=<ADMIN_IDS_COMMA_SEPARATED>
TELEGRAM_BOT_URL=http://bot:8001
ENABLE_INLINE_BUTTONS=true

# Frontend URL (Staging)
FRONTEND_URL=https://devrecyclic.jarvos.eu

# API Configuration
VITE_API_URL_STAGING=/api
API_V1_STR=/v1
ROOT_PATH=

# Frontend Environment Badge (B50-P7)
# Cette variable permet d'afficher le badge "Environnement de test" en staging
VITE_ENVIRONMENT=staging

# Application Environment
ENVIRONMENT=staging

# CORS Settings (Staging)
ALLOWED_HOSTS=devrecyclic.jarvos.eu
CORS_ALLOW_ORIGINS=https://devrecyclic.jarvos.eu
BACKEND_CORS_ORIGINS=https://devrecyclic.jarvos.eu

# Email Service (Brevo)
# Clé API Brevo pour l'envoi d'emails (obligatoire pour les fonctionnalités email)
BREVO_API_KEY=<STAGING_BREVO_API_KEY>
# Secret webhook Brevo pour vérifier les callbacks (optionnel)
BREVO_WEBHOOK_SECRET=<STAGING_WEBHOOK_SECRET>
# Nom de l'expéditeur affiché dans les emails
EMAIL_FROM_NAME=Recyclic (Staging)
# Adresse email de l'expéditeur (doit être vérifiée dans Brevo)
EMAIL_FROM_ADDRESS=noreply@staging.recyclic.jarvos.eu
# Adresse email par défaut pour les tests (optionnel, configurable via l'interface admin)
DEFAULT_EMAIL_RECIPIENT=

# Cash session reports
CASH_SESSION_REPORT_DIR=/app/reports/cash_sessions
CASH_SESSION_REPORT_RECIPIENT=<TEST_EMAIL>
CASH_SESSION_REPORT_TOKEN_TTL_SECONDS=900
CASH_SESSION_REPORT_RETENTION_DAYS=7

# kDrive Sync (Optional)
KDRIVE_WEBDAV_URL=https://kdrive.example.com/remote.php/webdav
KDRIVE_WEBDAV_USERNAME=<KDRIVE_USERNAME>
KDRIVE_WEBDAV_PASSWORD=<KDRIVE_APP_PASSWORD>
KDRIVE_REMOTE_BASE_PATH=/Recyclic/staging/exports
KDRIVE_SYNC_ENABLED=false
KDRIVE_SYNC_INTERVAL_SECONDS=3600
KDRIVE_RETRY_DELAY_SECONDS=5
KDRIVE_MAX_RETRIES=3

# Backup Configuration
BACKUP_REMOTE_HOST=<BACKUP_SERVER_HOST>
BACKUP_REMOTE_USER=<BACKUP_USER>
BACKUP_REMOTE_PATH=/backups/recyclic/staging
BACKUP_RETENTION_DAYS=7
NOTIFICATION_EMAIL=<ADMIN_EMAIL>
NOTIFICATION_TELEGRAM_TOKEN=<BOT_TOKEN_FOR_NOTIFICATIONS>
NOTIFICATION_TELEGRAM_CHAT_ID=<ADMIN_CHAT_ID>

# Docker Image Tags (for versioning)
API_IMAGE_TAG=latest
BOT_IMAGE_TAG=latest
FRONTEND_IMAGE_TAG=latest
API_MIGRATIONS_IMAGE_TAG=latest

# First Super Admin bootstrap (création automatique au démarrage si défini)
FIRST_SUPER_ADMIN_USERNAME=admin
FIRST_SUPER_ADMIN_PASSWORD=changeme

# Legacy Import - LLM Fallback (B47-P5)
# Provider LLM utilisé pour le fallback sur les catégories legacy.
# En production, mettre explicitement "openrouter" pour activer, ou laisser vide / "none" pour désactiver.
LEGACY_IMPORT_LLM_PROVIDER=
# Modèle LLM OpenRouter utilisé pour le mapping de catégories (voir https://openrouter.ai/models).
# Exemple : mistral-7b-instruct, qwen2.5-7b-instruct, etc.
LEGACY_IMPORT_LLM_MODEL=
# Taille de batch pour l'envoi des catégories unmapped au LLM (20 recommandé).
LEGACY_IMPORT_LLM_BATCH_SIZE=20
# Clé API OpenRouter (NE PAS commiter de valeur réelle).
OPENROUTER_API_KEY=<PRODUCTION_OPENROUTER_API_KEY>
# URL de base de l'API OpenRouter (conserver la valeur par défaut sauf besoin spécifique).
OPENROUTER_API_BASE_URL=https://openrouter.ai/api/v1
