/**
 * Clés `presentation_labels` du ContextEnvelope (OpenAPI) consommées par le shell —
 * présentation uniquement ; pas d’autorisation dérivée de ces libellés.
 */
export const CONTEXT_ACTIVE_SITE_DISPLAY_NAME_KEY = 'context.active_site_display_name' as const;
