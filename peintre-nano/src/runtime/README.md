# Runtime

Orchestration future du moteur de composition (résolution manifests, registre, rendu déclaratif).  
Story 3.0 : `conceptual-artifacts.ts` exporte le type `PeintreTruthStack` ; `conceptual-artifacts.stub.ts` fournit le stub typé (tests + side-effect `void` au chargement via `main.tsx`).
