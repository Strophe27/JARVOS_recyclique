# Matrice de parite UI — initialisation (pilotes Legacy -> Peintre_nano)

Date : 2026-04-10

## Objet

Amorcer la matrice normative de parite observable entre le frontend legacy (`recyclique-1.4.4`, reference UX sur `http://localhost:4445`) et `Peintre_nano`, avec trois lignes pilotes stables.

Les cellules non encore verifiees restent explicitement ouvertes ; aucune URL, `operationId`, cle `CREOS` ou preuve n'est inventee ici.

**Cadrage** : `references/artefacts/2026-04-10_02_cadrage-parite-ui-legacy-peintre.md`

## Regles d'usage

1. Le legacy sur `http://localhost:4445` sert a observer routes, shell, chrome et etats visibles ; toute precision manquante est renseignee apres verification sur cette instance, pas par supposition.
2. Toute ligne qui tend vers `Valide` ou `Ecart accepte` doit lier `OpenAPI` + `ContextEnvelope` + artefact `CREOS` reviewable, conformement au cadrage.
3. Tant que `Preuve / validation` est vide, la ligne ne peut pas passer a `Valide`.
4. Les formulations `A confirmer sur http://localhost:4445` doivent etre remplacees par des valeurs exactes une fois le slice verrouille.
5. Colonne **`Equiv. utilisateur / derogation PO`** (entre `Story / lien` et `Ecarts / decisions`) : renseigner **`OK`**, **`Derogation PO (<ref>)`**, ou **`Hors scope (<ref>)`** ; **au plus une** `Derogation PO` par ligne pilote ; pour un statut cible **`Valide`**, cette cellule doit contenir **`OK`** ou une **`Derogation PO`** explicite (date + lien ou story).
6. Les lignes créées **avant** le 2026-04-12 peuvent porter **`N/A historique`** jusqu'à leur prochaine révision ; toute **nouvelle** ligne ou **révision** de ligne après cette date applique la règle 5.
7. Gate **preuve visuelle** (PR ou story **Epic 11 / 13** caisse, ou ligne `ui-pilote-03*` caisse) : MCP Cursor **`user-chrome-devtools`** (legacy `localhost:4445` vs Peintre `localhost:4444`, memes intentions) — voir `guide-pilotage-v2.md` § *Règle caisse Peintre vs legacy (2026-04-12)*.

## Matrice (lignes pilotes)

| Cle | Perimetre | Reference legacy | Artefact CREOS | OpenAPI | Contexte | Critere de parite | Preuve / validation | Statut | Story / lien | Equiv. utilisateur / derogation PO | Ecarts / decisions |
|-----|-----------|------------------|----------------|---------|----------|-------------------|---------------------|--------|--------------|-------------------------------------|-------------------|
| `ui-pilote-01-login-public` | `flux / page` | Observe sur `http://localhost:4445/login` (Chrome DevTools MCP, 2026-04-10) : document `RecyClique`, heading `Connexion`, champs `Nom d'utilisateur` et `Mot de passe`, bouton `Se connecter`, lien `Mot de passe oublié ?` vers `http://localhost:4445/forgot-password` ; chargement page = assets Vite + `Login.tsx` ; pas de requete XHR au chargement seul | `contracts/creos/manifests/page-login-public.json` (`page_key` `login-public`) + widget `auth.live.public-login` ; shell `LiveAuthShell` compose ce manifeste hors session (`VITE_LIVE_AUTH`) | Chaine reviewable : `recyclique_auth_login` puis `recyclique_users_getContextEnvelope` (pas `GET /v1/users/me` comme source de contexte) ; ecart legacy : appels additionnels observes en prod (`/users/me/permissions`, `activity/ping`) — hors YAML minimal — documentes comme gaps | Shell public Peintre_nano : route `/login` hors session puis `replaceState` vers `/dashboard` apres succes (canon CREOS story 11.2) ; lien mot de passe oublie present (navigation seule, flux complet hors scope story) | Alignement intentions : memes libelles principaux que le legacy via `widget_props` ; tests `public-login-widget-11-1` + contrat `page-login-public-creos-11-1` | Preuve legacy : snapshot a11y DevTools sur `localhost:4445/login` ; preuve Peintre_nano : memes textes via CREOS + tests unitaires/contrat ; parcours reseau succes a reproduire avec compte reel sous `VITE_LIVE_AUTH` | `En cours` | Story 11.1 | N/A historique | Gaps : forgot/reset/signup complets, `activity/ping` comme critere separe ; ne pas confondre endpoints legacy additionnels avec la chaine OpenAPI minimale du slice |
| `ui-pilote-02-dashboard-unifie-standard` | `page / navigation` | Chrome DevTools MCP (2026-04-10) sur legacy `http://localhost:4445/` : sans session → redirection effective vers `/login` (heading Connexion, etc.) ; apres login, shell avec logo RecyClique, lien **Tableau de bord** vers `http://localhost:4445/` (pas `/dashboard`) ; clic **Tableau de bord** sans session → retour login observe. Contenu riche post-login : `Caisse`, `Reception`, `Administration`, filtres stats, etc. (inventaire existant matrice) | Ancrage reviewable : `navigation-transverse-served.json` (`transverse-dashboard` → `/dashboard`) + `page-transverse-dashboard.json` | Observe en reseau apres login : `GET /api/v1/categories/`, `GET /api/v1/cash-sessions/stats/summary`, `GET /api/v1/stats/reception/summary`, `GET /api/v1/stats/reception/by-category`, `GET /api/v1/stats/sales/by-category` ; cote reviewable, garder `recyclique_users_getContextEnvelope`, `recyclique_users_refreshContextEnvelope`, `recyclique_users_getMyPermissions` comme structurels et traiter les appels stats comme inventaire legacy a mapper | Shell `standard`, session authentifiee, navigation standard visible ; pas de `requiredPermission` explicite sur la route legacy `/` ; role observe = `admin` ; signaux fines du bandeau session restent a confirmer | Meme ordre macro et meme valeur d'usage que le legacy : accueil authentifie, navigation standard, blocs principaux et filtres temporels ; etats visibles majeurs conformes ; la tolerance partielle ne vaut que pour le comportement legacy isole sur les ventes par categorie, pas pour tout le chargement | Preuves DevTools : `GET /` sans session → `/login` ; nav authentifiee lien Tableau de bord → `/` ; Peintre_nano `VITE_LIVE_AUTH` : post-login `replaceState` → `/dashboard`, `RuntimeDemoApp` + `RootShell` + nav, bandeau bac à sable masqué ; tests `live-auth-shell-11-2`, `runtime-demo-live-auth-11-2` | `Ecart accepte` | Story 11.2 | Derogation / ecart documente (voir colonne Ecarts) | **Decision** : canon CREOS / pilote live = post-login **`/dashboard`** ; legacy ancre l'equivalent sur **`/`** — ecart accepte et documente (pas de fusion silencieuse). Lien legacy Tableau de bord → `/` coherent avec cette decision |
| `ui-pilote-03-caisse-vente-kiosk` | `flux / page` | **Sans session (DevTools MCP, 2026-04-10)** : navigation vers `http://localhost:4445/caisse` puis `http://localhost:4445/cash-register/sale` → **redirection effective** vers `http://localhost:4445/login` (snapshot a11y : heading `Connexion`, champs alignes login public) ; document [200] possible selon SPA, mais l’URL cible observable est `/login`. **Reseau** : aucune requete XHR/fetch listee sur ces navigations sans session. **Avec session** (brownfield) : hub `/caisse` puis kiosque `/cash-register/sale`, `Sale` plein ecran — preuve vente / `POST /v1/sales/` : gap HITL session reelle | Ancrage reviewable : `/caisse` + `page_key` `cashflow-nominal` (`page-cashflow-nominal.json`). Pas d’entree `NavigationManifest` pour `/cash-register/sale` — **decision ecrite** : `peintre-nano/docs/03-contrats-creos-et-donnees.md` § routage 11.3 ; alias **runtime** vers le meme `cashflow-nominal`, `RootShell` `hideNav`, sans second PageManifest | Meme inventaire API caisse qu’apres login sur le legacy (session ouverte) : `cash-registers/status`, `cash-sessions/*`, `stats/live`, `categories`, `presets`, ventes — a lier aux `operationId` OpenAPI en revue ; pas observe sur la capture sans session | Peintre : `/caisse` = nav visible ; `/cash-register/sale` = alias → memes slots CREOS + nav masquee + bac a sable masque ; `syncSelectionFromPath` → entree `cashflow-nominal` ; garde = `ContextEnvelope` + `resolvePageAccess` | Alignement intention : parcours nominal borne ; pas de metier recree dans le moteur ; tests `runtime-demo-cash-register-sale-kiosk-11-3`, `root-shell` (hideNav) | Preuves : redirections sans session + absence XHR/fetch (parent Story Runner) ; Peintre_nano alias + tests ; **gap** : capture kiosque authentifiee + point reseau `POST /v1/sales/` | `Ecart accepte` | Story 11.3 | **Derogation PO** (stories **11.3**–**13.6** ; col. **Ecarts** ci-contre ; artefact `2026-04-12_04_certification-caisse-equivalence-legacy-13-6.md`) | **Decision** : CREOS canon = hub **`/caisse`** + `cashflow-nominal` ; **`/cash-register/sale`** = alias legacy **runtime uniquement** — pas d’equivalence contractuelle implicite. Revision CREOS separee si formalisation route dediee |
| `ui-pilote-03a-caisse-adjacents-kiosque` | `flux / page` | **Legacy** (Chrome DevTools MCP, 2026-04-11, `http://localhost:4445`, compte **admin** / `Changeme123!`) : **`/caisse`** apres chargement — heading `Sélection du Poste de Caisse`, poste `La Clique Caisse 1` + statut `FERMÉE` + bouton `Ouvrir`, sections virtuel / différé ; **`/cash-register/session/open?register_id=…`** — heading `Ouverture de Session de Caisse`, libellé `Fond de caisse initial`, boutons `Annuler` et `Ouvrir la Session` ; shell transverse identique (liens Tableau de bord, Caisse, Réception, Administration). **Reseau** (extrait hub + session open) : `GET /api/v1/users/me`, `GET /api/v1/users/me/permissions`, `GET /api/v1/cash-registers/status`, `GET /api/v1/cash-registers/{id}`, `GET /api/v1/cash-sessions/status/{register_id}`, `POST /api/v1/activity/ping`. **Flux « Ouvrir »** : preuve partielle sur selection poste + bouton `Ouvrir` visible ; reprise complete POST session hors scope 13.1 (pas d’exigence e2e ici). | Ancrage identique pilote 03 : `page-cashflow-nominal.json` (`widget_props` surface hub + surcouche presentationnelle alias session open, § 13.1 doc 03) + entree nav `cashflow-nominal` → `/caisse` ; **pas** d’entree `NavigationManifest` pour `/cash-register/session/open` — alias runtime § 13.1 dans `peintre-nano/docs/03-contrats-creos-et-donnees.md` | Mapper les GET legacy ci-dessus aux `operationId` du contrat `contracts/openapi/recyclique-api.yaml` en revue ; cote Peintre (reload `http://localhost:4444/cash-register/session/open?register_id=…`, 2026-04-11) : `GET /api/v1/users/me/context`, `GET /api/v1/cash-sessions/current`, `GET /api/v2/exploitation/live-snapshot` + inventaire elargi sur premier chargement depuis `/dashboard` (categories, stats) — **ajuster** selon captures reelles | `RuntimeDemoApp` : alias session open → `cashflow-nominal`, **nav visible** (contrairement au kiosque `/cash-register/sale`) ; composition `buildPageManifestRegions` ; pas de permissions « au feeling » hors `ContextEnvelope` / `resolvePageAccess` | Meme intention qu’avant kiosque : rester dans le workspace caisse sur URL legacy session open ; ne pas afficher le dashboard transverse a la place | Snapshots DevTools legacy + Peintre (post-fix) sur chemins ci-dessus ; tests unitaires + e2e `runtime-demo-cash-register-session-open-13-1`, `cash-register-session-open-13-1.e2e` | `Ecart accepte` | Story 13.1 | **Derogation PO** (stories **13.1** / **13.6** ; col. **Ecarts** ci-contre ; artefact `2026-04-12_04_certification-caisse-equivalence-legacy-13-6.md`) | **UI (2026-04-11)** : parite titres / CTA / rangée poste + statut + navigation `Ouvrir` → alias session ; prefill `register_id` URL ; **gap** : libelle poste convivial (ex. *La Clique Caisse 1*) tant que l’OpenAPI / enveloppe ne fournit pas de libelle reviewable pour le `register_id`. **Correlation** : legacy `users/me` + permissions + statuts caisse vs Peintre `users/me/context` + `cash-sessions/current` + `live-snapshot` |
| `ui-pilote-03b-caisse-virtuelle-legacy-urls` | `flux / page` | **Legacy** (Chrome DevTools MCP, 2026-04-11, `http://localhost:4445`, **admin** / `Changeme123!`) : hub **`/caisse`** — carte **Caisse Virtuelle** (badge `SIMULATION`, bouton **`Simuler`**) ; clic → URL **`/cash-register/virtual/session/open?register_id=31d56e8f-08ec-4907-9163-2a5c49c5f2fe`**, heading **`Ouverture de Session de Caisse`**, champ **`Fond de caisse initial`**, boutons **`Annuler`** / **`Ouvrir la Session`**, shell transverse. **Code** : `CashRegisterDashboard.tsx` (`handleVirtualCashRegister`), `OpenCashSession.tsx` (`basePath` `/cash-register/virtual`), `App.jsx` routes `virtual/session/open`, `virtual/sale` (kiosque). **Reseau** sur session open virtuel (extrait) : `GET …/users/me`, `…/permissions`, `…/cash-registers/status`, `…/cash-registers/{id}` (plus `activity/ping` hors inventaire minimal). | Meme **`page_key`** **`cashflow-nominal`** (`page-cashflow-nominal.json`) ; **pas** d’entree `NavigationManifest` pour `/cash-register/virtual/*` — alias runtime § 13.2 `peintre-nano/docs/03-contrats-creos-et-donnees.md` | Chaine reviewable inchangée vs pilote 03 (OpenAPI caisse / session / registres) ; pas de nouvelle operation inventee | `RuntimeDemoApp` : `/cash-register/virtual/session/open` + `/cash-register/virtual/sale` (kiosque) + hub **`/cash-register/virtual`** → `cashflow-nominal` ; surcouche `widget_props` session identique 13.1 ; CTA hub **`Simuler`** → navigation URL prefix **`/cash-register/virtual/session/open`** (`CaisseBrownfieldDashboardWidget`) | Meme intention navigation et libelles surface ouverture que le legacy observe ; vente en kiosque alignee `App.jsx` (`virtual/sale`) | Preuves DevTools legacy (snapshots + URL) ; Peintre : tests `runtime-demo-cash-register-variants-13-2`, `cash-register-variants-13-2.e2e` ; parite navigateur `localhost:4444` apres build aligne routing (meme compte) | `Ecart accepte` | Story 13.2 | **Derogation PO** (stories **13.2** / **13.6** ; col. **Ecarts** ci-contre ; artefact `2026-04-12_04_certification-caisse-equivalence-legacy-13-6.md`) | **Hub `/cash-register/virtual`** : le legacy n’affiche que la carte virtuelle (titre *Caisse Virtuelle - Mode Entraînement*) ; Peintre reutilise la **meme** composition hub CREOS que `/caisse` (cartes reel + variantes si permissions) — ecart **accepte** (meme `page_key`, intention navigation session/vente prioritaire). **Annuler** Peintre → hub `/caisse` (CREOS) vs legacy retour `/cash-register/virtual` — documente ici. |
| `ui-pilote-03c-caisse-saisie-differee-legacy-urls` | `flux / page` | **Legacy** (Chrome DevTools MCP, 2026-04-11, `http://localhost:4445`, **admin** / `Changeme123!`) : hub **`/caisse`** — carte **Saisie différée** (badge `ADMIN`, bouton **`Accéder`**) ; clic → **`/cash-register/deferred/session/open?register_id=31d56e8f-08ec-4907-9163-2a5c49c5f2fe`**, heading **`Ouverture de Session de Caisse`**, controles **`Date du cahier`** (date réelle de vente) + **`Fond de caisse initial`**, CTA identiques. **Code** : `CashRegisterDashboard.tsx` (`handleDeferredCashRegister`), `OpenCashSession.tsx` (`basePath` `/cash-register/deferred`), `App.jsx` `deferred/session/open`, `deferred/sale` (kiosque). Racine **`/cash-register/deferred`** : redirection legacy vers `…/session/open`. | Idem **`cashflow-nominal`** + alias runtime § 13.2 doc 03 | Idem pilote 03 ; champ date cahier = surface legacy (pas nouvelle regle metier dans Peintre) | `RuntimeDemoApp` : `/cash-register/deferred/session/open`, `/deferred/sale` (kiosque), normalisation **`/cash-register/deferred`** → `…/session/open` ; intro `widget_props` mention **cahier** sur branche différée ; CTA **`Accéder`** → prefix URL **`/cash-register/deferred/session/open`** | Intention URLs + shell non kiosque sur session open ; kiosque sur `deferred/sale` comme legacy | Preuves DevTools legacy (URL + snapshot champs *Date du cahier*) ; Peintre : tests 13.2 (fichiers ci-dessus) | `Ecart accepte` | Story 13.2 | **Derogation PO** (stories **13.2** / **13.6** ; col. **Ecarts** ci-contre ; artefact `2026-04-12_04_certification-caisse-equivalence-legacy-13-6.md`) | **Gap borne UI** : le legacy expose **Date du cahier** (spinbuttons jour/mois/an dedies) en premier bloc sur `OpenCashSession` différé ; Peintre conserve le **formulaire brownfield** unique (`datetime-local` + modes dans le widget) sur la meme page manifeste — parite **chemin + titre + fond + CTA**, pas copie du widget date Mantine legacy. **Correlation reseau** : meme famille que 03a (`users/me`, `permissions`, `cash-registers/status`, etc.). |
| `ui-pilote-03d-caisse-session-close-legacy-urls` | `flux / page` | **Legacy** (Chrome DevTools MCP, 2026-04-12, `http://localhost:4445`, **admin** / `Changeme123!`) : navigation **`/cash-register/session/close`** sans session caisse ouverte → apres chargement, **redirection effective** vers **`/caisse`** (heading **`Sélection du Poste de Caisse`**). **Code** : `CloseSession.tsx` (titre **Fermeture de Caisse**, chargement **Chargement des données de la session...**, resume / controle montants / step-up via store), `Sale.tsx` (`handleCloseSession` → `` `${basePath}/session/close` `` avec `basePath` reel / **`/cash-register/virtual`** / **`/cash-register/deferred`**), `App.jsx` routes **`/cash-register/session/close`**, **`/cash-register/virtual/session/close`**, **`/cash-register/deferred/session/close`**. **Reseau** (extrait page close avant redirection) : `GET …/users/me`, `POST …/activity/ping` ; `GET /v1/cash-sessions/current` (ou equivalent store) — correler OpenAPI `recyclique_cashSessions_getCurrentOpenSession` ; cloture effective : **`POST /v1/cash-sessions/{id}/close`** (`recyclique_cashSessions_closeSession`, headers step-up / idempotence Story 2.4 / 6.7). | **`page_key` `cashflow-nominal`** (`page-cashflow-nominal.json`) ; **pas** d'entree `NavigationManifest` pour `…/session/close` — alias runtime § 13.3 `peintre-nano/docs/03-contrats-creos-et-donnees.md` | Chaine reviewable : session courante + close session (pas de logique metier inventee dans le routeur) | `RuntimeDemoApp` : les trois prefixes `…/session/close` → `cashflow-nominal`, **nav visible** (pas kiosque) ; `widget_props` **`presentation_surface: session_close`** + `session_close_sale_path` ; widget **`CaisseSessionCloseSurface`** consomme `getCurrentOpenCashSession` / `postCloseCashSession` (`cash-session-client.ts`) | Meme intention : cloture bornee observable + retour vente sur branche URL ; redirection hub **`/caisse`** si pas de session ouverte (aligne legacy) | Preuves DevTools legacy + Peintre (Story Runner BMAD **2026-04-12**, `navigate_page` / `take_snapshot` / `list_network_requests`, memes hotes et compte) : sans session → hub **`/caisse`** ; session ouverte sans vente → ecran **Fermeture de Caisse** + bloc **Session sans transaction** + reseau `GET …/cash-sessions/{id}` ; tests `runtime-demo-cash-register-session-close-13-3`, `cash-register-session-close-13-3.e2e` ; gates peintre-nano lint / test / build **OK** | `Ecart accepte` | Story 13.3 | **Derogation PO** (stories **13.3** / **13.6** ; col. **Ecarts** ci-contre ; artefact `2026-04-12_04_certification-caisse-equivalence-legacy-13-6.md`) | **Peintre** (meme manifeste CREOS) vs legacy styled-components : parite **libelles et blocs** (resume, controle montants, session vide). **PIN** saisi en UI pour **POST** `close` (**ecart accepte** vs legacy sans champ PIN sur le bloc *session vide* — OpenAPI step-up Stories **2.4** / **6.7**, pas invention metier front). **Titres** : **h1** *Fermeture de Caisse*, **h3** *Session sans transaction*, **h2** resume / controle — alignes `CloseSession.tsx` (**correctif** 2026-04-12 `CaisseSessionCloseSurface.tsx`). **POST close** : declenchable apres saisie PIN (valeur test **1234** uniquement env demo). **Retour vente** : `session_close_sale_path` comme `Sale.tsx` (`/cash-register/sale`, `virtual/sale`, `deferred/sale`). |
| `ui-pilote-03e-rcn-01-hub-caisse` | `page / hub` | **Legacy** (Chrome DevTools MCP, 2026-04-12, `http://localhost:4445/caisse`, session authentifiee recette) : heading **`Sélection du Poste de Caisse`**, carte poste (nom + lieu + **FERMÉE** + **Ouvrir**), **Caisse Virtuelle** (**SIMULATION**, **Simuler**), **Saisie différée** (**ADMIN**, **Accéder**) ; nav transverse ; etat **chargement** global **LoadingOverlay** ; menu **Menu de gestion** si super-admin (`CashRegisterDashboard.tsx`). **Reseau** (extraits listes) : `GET …/users/me`, `GET …/users/me/permissions`, `GET …/cash-registers/status`, `POST …/activity/ping`. | **`page_key` `cashflow-nominal`** (`page-cashflow-nominal.json`) + `withCashflowNominalCaisseHubPresentation` (`presentation_surface: caisse_hub`) — § RCN-01 `peintre-nano/docs/03-contrats-creos-et-donnees.md` | Donnees postes : `GET /v1/cash-registers/status` (`getCashRegistersStatus`) ; session courante : `GET /v1/cash-sessions/current` — pas d’autorite metier parallele dans le widget | `RuntimeDemoApp` : `/caisse` → hub compact sans wizard ni ticket lateral (`suppressCashflowNominalWorkspaceSaleAndAside`) ; CTA **Ouvrir** / **Reprendre** → prefixes legacy via `cash_register_hub_base_path` | Checklist hub : titres, cartes, CTA, etats chargement / liste vide (messages d’alerte existants) ; preuves navigateur legacy + Peintre | Preuves DevTools **2026-04-12** : snapshots **`/caisse`** sur **4445** et **4444** ; reseau Peintre : `GET …/users/me/context`, `GET …/cash-sessions/current`, `GET …/cash-registers/status` ; tests `runtime-demo-cash-register-hub-rcn-01-13-4`, `cash-register-hub-rcn-01-13-4.e2e` | `Ecart accepte` | Story 13.4 | **Derogation PO** (stories **13.4** / **13.6** ; col. **Ecarts** ci-contre ; artefact `2026-04-12_04_certification-caisse-equivalence-legacy-13-6.md`) | **Intro** sous titre : **reviewable** CREOS (`workspace_intro`) vs legacy sans paragraphe — **ecart accepte**. **Menu admin postes** legacy : **gap** assumé (hors widget hub). **Badge** statut poste : memes libelles accessibles **Ouverte** / **Fermée** (rendu a11y **OUVERTE** / **FERMÉE** selon styles). **Loading** : **LoadingOverlay** zone cartes Peintre vs overlay plein legacy — **ecart accepte** structurel (meme intention de blocage interaction). |
| `ui-pilote-03f-rcn-02-hub-vers-vente-kiosk` | `flux / transition` | **Legacy** : depuis **`/caisse`**, poste en session ouverte, **Reprendre** → **`/cash-register/sale`** ; vente plein cadre, nav principale masquee (`App.jsx` / kiosque). Sequence URL ordonnee documentee par DevTools (pas d'URL inventee). | Meme **`page_key`** **`cashflow-nominal`** ; alias runtime **`/cash-register/sale`** § 11.3 + § RCN-02 `peintre-nano/docs/03-contrats-creos-et-donnees.md` ; hub → sale via **`spaNavigateTo`** / **`handleHubResumeRegister`** dans **`CaisseBrownfieldDashboardWidget`** | Chaine reviewable inchangee (pas d'endpoint invente pour la transition) | `RuntimeDemoApp` : `hideNav` + `cash-register-sale-kiosk` sur **`/cash-register/sale`** ; pas de double hub + vente (`suppressCashflowNominalWorkspaceSaleAndAside` false sur sale) | Equivalence : URLs hub → sale reel ; disparition nav transverse ; plein cadre kiosque ; chargements hub deja RCN-01 | Preuves MCP **user-chrome-devtools** Story 13.5 DS : **`/caisse`** puis action jusqu'a **`/cash-register/sale`** sur **4445** et **4444** ; tests `runtime-demo-cash-register-hub-to-sale-rcn-02-13-5`, `cash-register-hub-to-sale-rcn-02-13-5.e2e` | `Ecart accepte` | Story 13.5 | **OK** (certification **13.6** 2026-04-12 ; artefact `2026-04-12_04_certification-caisse-equivalence-legacy-13-6.md` + preuves `2026-04-12_04_certification-13-6-preuves/`) | Bandeau bac a sable masque sur alias kiosque en demo hors **VITE_LIVE_AUTH** (§ 11.3) ; **extension 13.6** : meme principe sur **tout** chemin `cashflow-nominal` demo (`RuntimeDemoApp`). **Toolbar** live : absente quand nav masquee (kiosque). |
| `ui-pilote-14-01-admin-shell-perimetre` | `navigation / shell` | **Legacy** : `recyclique-1.4.4/frontend/src/components/AdminLayout.jsx` — barre verte `#2e7d32`, liens Tableau de bord (`/`), Caisse, Réception, Administration (`/admin`), menu utilisateur ; `DashboardHomePage` pour le corps (hors périmètre strict 14.1). | Peintre : barre verte auth live (stories 5.x) ; **14.1** : `LiveAdminPerimeterStrip` sur `/admin*` + `TransverseHubLayout` `shellAdmin` ; hub CREOS `page-transverse-admin-placeholder.json` titre aligné « Tableau de bord d'administration ». | `recyclique_users_getContextEnvelope` ; `presentation_labels.context.active_site_display_name` (serveur : `context_envelope_service` + `Site.name`) | Admin + `transverse.admin.view` + contexte site | Shell crédible + périmètre site lisible ou gap sans UUID | **MCP** : erreur instance Chrome occupée lors du run orchestrateur (2026-04-12) — preuve navigateur à rejouer ; tests `live-admin-perimeter-strip-14-1` ; `pytest` `test_context_presentation_labels_nav_transverse_dashboard` (assertion libellé site) | `A valider` | Story 14.1 | N/A historique | **Gap** : menu **Mon profil** legacy (`/profil`) non manifesté CREOS. **Ecart** : lien Tableau de bord legacy `/` vs Peintre `/dashboard` (doc 03 § 11.2). |
| `ui-pilote-14-02-admin-parametres-simples` | `page` | **Legacy** : écrans bornés type `/admin/sites`, `/admin/settings` (super-admin), etc. — hors scope total ; lot Peintre `/admin/access`, `/admin/site` (Story 5.4). | `page-transverse-admin-access-overview.json`, `page-transverse-admin-site-overview.json` + nav `navigation-transverse-served.json` | Meme chaine permissions `transverse.admin.view` ; pas de `data_contract` sur ces pages | Hubs cadrage + libellés nav alignés « Accès et visibilité » / « Site et périmètre » (`creos_nav_presentation_labels.py`) | — | Preuves MCP + comparaison textes ; gates Peintre | `En cours` | Story 14.2 | N/A historique | Pas de duplication de `/admin/settings` super-admin ; persistance métier = gaps OpenAPI explicites dans les slots `demo.text.block`. |
| `ui-pilote-14-03-admin-supervision-simple` | `page` | **Legacy** : `DashboardHomePage.jsx` — stats jour (`/v1/cash-sessions/stats/summary`, `/v1/stats/reception/summary`), en-têtes `HeaderAlerts` / `HeaderCA` / `HeaderUser`, grille navigation principale. | Aucun widget `data_contract` reviewable équivalent dans le lot CREOS admin actuel | `recyclique_cashSessions_statsSummary` (nom exact à prendre sur OpenAPI), stats réception | Admin | Parité blocs supervision ou gap documenté | Preuves MCP + réseau legacy ; côté Peintre : gap assumé jusqu'à branchement contrats | `Backlog` | Story 14.3 | N/A historique | **Gap principal** : pas d'invention de dashboard métier dans Peintre ; livraison reportée sur slices OpenAPI + widgets officiels. |

---

## Extension story 13.7 — blueprint interne kiosque (2026-04-12)

**Référence** : `references/artefacts/2026-04-12_06_blueprint-portage-kiosque-13-7.md`  
**Taxonomie (blueprint)** : le tableau « Statut ligne matrice » du blueprint utilise les **quatre** libellés AC #1 de la story **13.7** (`Reproduire tel quel`, `Adapter langage Peintre (justification)`, `Reporter / gap contrat`, `Hors scope (ref)`) et une **légende de mapping** depuis l’ancien vocabulaire interne ; les colonnes **Statut** / **Equiv.** des lignes `ui-pilote-03*` **ci-dessus** ne sont pas modifiées par cette extension.  
**Périmètre** : décomposition **intra** frame vente kiosque (wizard legacy `Sale` + `SaleWizard` + `Ticket` + `FinalizationScreen` + `Numpad` + en-têtes / KPI) vs composition Peintre `cashflow-nominal` (`cashflow-nominal-wizard`, `caisse-current-ticket`, surcouches `RuntimeDemoApp`, `widget_props` kiosque).  
**Lignes matrice concernées** : `ui-pilote-03-caisse-vente-kiosk`, `ui-pilote-03b-*` / `03c-*` (chemins `…/sale` kiosque), `ui-pilote-03f-rcn-02-hub-vers-vente-kiosk`. Les colonnes **Statut** et **Equiv. utilisateur / derogation PO** des lignes existantes restent **inchangées** par cette extension (pas de decision PO nouvelle) ; le blueprint alimente la **story 13.8** (implementation) et la relecture PO du perimetre interne wizard / ticket / finalisation / clavier.

**Mise à jour Story 13.8 (impl. 2026-04-12)** : sur Peintre `http://localhost:4444/cash-register/sale`, le runtime injecte **`sale_kiosk_category_workspace`** sur **`cashflow-nominal-wizard`** ; grille catégories **`GET /v1/categories/`** + ticket slot **`aside`** inchangé. **Equiv. utilisateur** : intention « parcours catégorie → ligne » rapprochée du legacy ; écarts résiduels volontaires (blueprint) : **FlowRenderer** multi-étapes vs tablist unique, **pavé numérique** et **raccourcis AZERTY** non livrés dans ce tronçon (P1 / backlog clavier) ; en-tête session / KPI = lignes blueprint **Reporter / gap** ou hors scope sans contournement. **Preuve headless Peintre + protocole comparatif** (run Story Runner 2026-04-12) : `references/artefacts/2026-04-12_09_comparatif-headless-kiosque-13-8-run.md` + dossier `references/artefacts/2026-04-12_08_preuves-kiosque-13-8-headless/` (PNG + `signals-*.json` + `summary.json`) ; branche legacy virtual automatisée **non stabilisée** sur cet environnement — baseline legacy visuelle inchangée : ligne **`ui-pilote-03b-caisse-virtuelle-legacy-urls`** (MCP 2026-04-11).

---

## Extension Epic 15 — admin strict (Story 15.4, 2026-04-12)

**Périmètre** : routes SPA sous `/admin` dans `recyclique-1.4.4/frontend/src/App.jsx` (bloc `<Route path="/admin" …>`, 2026-04-12) croisées avec `recyclique-1.4.4/frontend/src/config/adminRoutes.js`. Tant que le livrable **15.1** (inventaire archive daté) n’est pas publié, cette liste fait office de **couverture sans omission silencieuse** dérivée du code ; toute divergence future avec 15.1 sera tracée en colonne *Ecarts*.

| Clé | Périmètre | Référence legacy | Artefact CREOS | OpenAPI | Contexte | Critère de parité | Preuve / validation | Statut | Story / lien | Equiv. utilisateur / dérogation PO | Ecarts / décisions |
|-----|-----------|------------------|----------------|---------|----------|-------------------|---------------------|--------|--------------|-------------------------------------|-------------------|
| `ui-admin-15-4-home-index-dashboard` | `page` | **`/admin`** (index) et **`/admin/dashboard`** → même composant `DashboardHomePage.jsx` que la ligne `ui-pilote-14-03`. | Story **18.1** : hub CREOS `page-transverse-admin-reports-hub.json` (`page_key` **`transverse-admin-reports-hub`**) pour l’entrée `/admin` + widget gap K + liens manifestés ; bloc legacy `admin.legacy.dashboard.home` inchangé côté données (**14.3**). | Stats et blocs : **dette** — noms `operationId` exacts à figer en **15.2** (cf. ligne 14-03). | `ProtectedRoute` `adminOnly` ; rôles admin hérités legacy. | Pas de double exigence : parité = celle de **14.3** + cohérence des deux chemins URL vers le même écran. | **Story 18.3** : relecture ligne **14-03** ; preuve automatisée hub `/admin` (e2e `navigation-transverse-5-1` + contrat `navigation-transverse-served-5-1`) + doc `03-contrats` § **Story 18.3** ; comparaison navigateur 4445/4444 **NEEDS_HITL** — `2026-04-12_07_preuve-parite-admin-surfaces-caisse-18-3-needs-hitl.md` (MCP indisponible au DS). | `En cours` | 15.4 / 14.3 / **18.1** / **18.3** | OK | Alias `/admin/dashboard` legacy vs index : pas d’écart fonctionnel attendu (même page). |
| `ui-admin-15-4-cash-session-detail` | `page` | **`/admin/cash-sessions/:id`** → `CashSessionDetail.tsx` (ventes session, actions sensibles ventes). | **`contracts/creos/manifests/page-admin-cash-session-detail.json`** (`page_key` `admin-cash-session-detail`) ; widget `admin-cash-session-detail` — aligné doc `03-contrats-creos-et-donnees.md` ; cohérence nav hub **`transverse-admin`** + liste manifestée **`/admin/session-manager`** (**Story 18.2**). | Familles ventes / sessions caisse (`recyclique_sales_*`, `recyclique_cashSessions_*`) — **corrélation exacte** réservée à **15.2**. | `adminOnly` + permissions transverse admin. | Liste ventes + corrections sensibles alignées contrat step-up (Epic **6.8**) quand portées. | **Story 18.3** : code legacy + manifeste ; contrat bundle `admin-cash-session-detail` (`navigation-transverse-served-5-1`) ; e2e URL profonde + selection hub (`navigation-transverse-5-1`) ; gouvernance OpenAPI `recyclique_cashSessions_getSessionDetail` (`recyclique-openapi-governance.test.ts`) ; MCP 4445/4444 **NEEDS_HITL** — `2026-04-12_07_preuve-parite-admin-surfaces-caisse-18-3-needs-hitl.md`. | `En cours` | 15.4 / **18.2** / **18.3** | OK | **Gap** : volume d’écrans legacy (édition poids, notes) vs périmètre widget reviewable — à resserrer en portage. |
| `ui-admin-15-4-reception-stats` | `page` | **`/admin/reception-stats`** → `ReceptionDashboard.tsx`. | **Story 19.1** : `navigation-transverse-served.json` + `page-transverse-admin-reception-stats.json` (`page_key` **`transverse-admin-reception-stats`**) + widget **`admin.reception.stats.supervision`** (slots **17.3**) ; lectures **`recyclique_stats_receptionSummary`**, **`recyclique_stats_receptionByCategory`**, **`recyclique_stats_unifiedLive`** via `dashboard-legacy-stats-client` ; point d’entrée hub **`admin-hub-link-reception-stats`**. | OpenAPI canon **16.4** : stats admin ; `recyclique_reception_statsLiveDeprecated` non utilisé (préférence successeur). | `adminOnly` → CREOS `transverse.admin.view` + `requires_site`. | Résumé + par catégorie + live unifié branchés ; graphiques Recharts / filtres avancés legacy = **dette nommée** ; supervision nominative sessions/opérateurs = **gap K** (pas de tableau fictif) → **19.2** / **Epic 16**. | **Story 19.3** : tests contrat + e2e `navigation-transverse-*` (entrée nav + URL profonde `/admin/reception-stats` + marqueurs `operation_id` + absence nav `/admin/reception-reports`) + unitaire `admin-reception-stats-supervision-widget.test.tsx` ; doc `03-contrats` § **19.1** / **19.3** ; comparaison navigateur 4445/4444 **NEEDS_HITL** — `2026-04-12_08_preuve-parite-pilotage-reception-19-3-needs-hitl.md`. | `En cours` | 15.4 / **19.1** / **19.3** | OK | Parité legacy visuelle (Recharts) **non** revendiquée ; pas de « parité OK » global. |
| `ui-admin-15-4-reception-reports` | `page` | **`/admin/reception-reports`** → `ReceptionReports.tsx` (exports CSV potentiels). | **Gap CREOS**. | Idem stats + exports — **15.2** ; flux sensibles **step-up** si applicable. | `adminOnly`. | Parité rapports + exports traçables. | **Story 19.3** : ligne **isolée** dans la matrice (pas fusionnée avec stats/sessions/ticket-detail) ; **hors critère** de succès parité pilotage **19.x** ; preuve navigateur **NEEDS_HITL** — `2026-04-12_08_preuve-parite-pilotage-reception-19-3-needs-hitl.md`. | `Backlog` | 15.4 / **19.3** (preuve) | **Hors scope (parité pilotage 19.x — Epic 16 / 15.2)** | Exports = surface **sensible** ; traiter avec ligne « sensibles » **15.2** ; ne pas compter comme « fait » pour **19.3**. |
| `ui-admin-15-4-reception-sessions` | `page` | **`/admin/reception-sessions`** → `ReceptionSessionManager.tsx`. | **Story 19.2** : `navigation-transverse-served.json` + `page-transverse-admin-reception-sessions.json` (`page_key` **`transverse-admin-reception-sessions`**) + widget **`admin.reception.tickets.list`** (slots **17.3**) ; liste **`recyclique_reception_listTickets`** ; KPIs legacy hors `ReceptionTicketSummary` = **gap K** dans le manifeste ; mutations/export **B** = exclus visibles (liste + détail). | `GET /v1/reception/tickets` + champs summary OpenAPI. | `adminOnly` → CREOS alignés. | Drill-down vers détail ticket manifesté. | **Story 19.3** : tests contrat (slot **contract-gap** texte export **B** reviewable) + e2e `navigation-transverse-*` (hub → sessions + `admin-reception-tickets-scope-note`) + doc `03` § **19.2** / **19.3** ; MCP **NEEDS_HITL** — `2026-04-12_08_preuve-parite-pilotage-reception-19-3-needs-hitl.md`. | `En cours` | 15.4 / **19.2** / **19.3** | OK | Bulk legacy hors OpenAPI : toujours **Epic 16**. |
| `ui-admin-15-4-reception-ticket-detail` | `page` | **`/admin/reception-tickets/:id`** → `ReceptionTicketDetail.tsx`. | **Story 19.2** : `page-admin-reception-ticket-detail.json` (`page_key` **`admin-reception-ticket-detail`**) + widget **`admin-reception-ticket-detail`** ; lecture **`recyclique_reception_getTicketDetail`** ; sélection nav hub **`transverse-admin`** (pas d'entrée nav par `ticket_id`, modèle **18.2**). | Détail + lignes alignés schéma OpenAPI. | `adminOnly`. | Actions sensibles : bloc explicite hors **19.2** (pas de boutons bulk/close/patch/export). | **Story 19.3** : e2e URL profonde UUID + `admin-reception-ticket-excluded-actions` + drill-down liste → détail ; doc § **19.3** ; MCP **NEEDS_HITL** — `2026-04-12_08_preuve-parite-pilotage-reception-19-3-needs-hitl.md`. | `En cours` | 15.4 / **19.2** / **19.3** | OK | Parité legacy actions : attend **Epic 16** si hors contrat step-up. |
| `ui-admin-15-4-reports-hub` | `page` | **`/admin/reports`** → `ReportsHub.tsx`. | **Story 18.1** : entrée nav **`transverse-admin`** sur **`/admin`** + `page-transverse-admin-reports-hub.json` + widget **`admin.reports.supervision.hub`** — structure / intentions + gap **K** (`/v1/admin/reports/` sans GET lecture) + liens uniquement vers routes déjà manifestées ; pas de données agrégées live. Bandeau compact sur hub **`/admin`**. Lien **`/admin/session-manager`** — **18.2**. | Rapports agrégés réels — **15.2** ; exports bulk classe **B** — **Epic 16** / rail **K**. | `adminOnly`. | Données et sous-rapports : **toujours en dette** tant que l’OpenAPI ne porte pas les lectures ; navigation secondaire observable = progrès partiel **18.1** uniquement. | **Story 18.3** : legacy structure (relecture code) ; Peintre — tests contrat + e2e hub + liens manifestes (`navigation-transverse-served-5-1`, `navigation-transverse-5-1`) ; doc § **Story 18.3** ; captures 4445/4444 **NEEDS_HITL** — `2026-04-12_07_preuve-parite-admin-surfaces-caisse-18-3-needs-hitl.md`. | `En cours` | 15.4 / **18.1** / **18.2** / **18.3** | OK | Parité données ≠ **18.1** ; liste sessions : placeholders **K** sous **18.2** (pas de données simulées). |
| `ui-admin-15-4-reports-cash-sessions` | `page` | **`/admin/reports/cash-sessions`** → `Reports.tsx`. | **Gap CREOS**. | Rapports caisse — **15.2**. | `adminOnly`. | Données et exports alignés backend. | Tests legacy `Reports.test.tsx` ; pas équivalence Peintre sans slice. | `Backlog` | 15.4 | OK | — |
| `ui-admin-15-4-users` | `page` | **`/admin/users`** → `Users.tsx` + `adminStore` / `adminService`. | **Gap CREOS** page dédiée (placeholder admin seul ne couvre pas la gestion utilisateurs). | CRUD / liste utilisateurs via services — **15.2** (ne pas déduire les `operationId` depuis seul le JSX). | `adminOnly` ; actions rôles potentiellement sensibles. | Liste, rôle, statuts visibles ; pas de seconde autorité UI. | Code legacy + **15.2** ; tests `Users.test.tsx` existants = preuve partielle code seulement. | `Backlog` | 15.4 | OK | Polling statuts : corréler endpoints réels en **15.2**. |
| `ui-admin-15-4-pending-users` | `page` | **`/admin/pending`** → `PendingUsers.tsx`. | **Slice CREOS** : `page-transverse-admin-pending.json` (`page_key` `transverse-admin-pending`) + nav servie ; widget `admin.pending-users.demo` (placeholder **sans** `data_contract`). **Story 17.3** : coquille `AdminListPageShell` + slots homogènes `admin.transverse-list.*` (gaps OpenAPI inchangés). | Flux utilisateurs en attente — **15.2** ; contrat live — **Epic 16** (`GET /v1/admin/users/pending` absent du OpenAPI canon au 2026-04-12). | `adminOnly` → garde-fous CREOS `transverse.admin.view` + `requires_site` (alignement hub admin **14.1**). | File d’attente : preuve UI = matrice + tests Peintre (`navigation-transverse-served-5-1`, e2e `navigation-transverse-5-1`, unitaire `admin-list-page-shell-17-3`). | `En cours` | 15.4 / **17.1** / **17.3** | OK | Données liste : **bloquées** tant que l’`operationId` n’est pas publié dans `contracts/openapi/recyclique-api.yaml` (pas d’invention côté Peintre). |
| `ui-admin-15-4-session-manager` | `page` | **`/admin/session-manager`** → `SessionManager.tsx`. | **Slice CREOS** **18.2** : `navigation-transverse-served.json` + `page-transverse-admin-session-manager.json` + widget **`admin.session-manager.demo`** (shell **17.3**, gaps **K** liste `GET /v1/cash-sessions/` + stats `…/stats/summary` absents YAML ; exports **B** exclus visuellement). | Sessions caisse admin — **15.2** (`cash-sessions` stats / listes) ; données live liste/KPIs → **Epic 16**. | `adminOnly`. | Filtres / analyses sessions alignés données réelles. | **Story 18.3** : legacy + tests Peintre (contrat + e2e parcours hub → session-manager → retour **Administration**, gap **K** + dette export **B**) ; § **18.2** / **18.3** `03-contrats-creos-et-donnees.md` ; MCP 4445/4444 **NEEDS_HITL** — `2026-04-12_07_preuve-parite-admin-surfaces-caisse-18-3-needs-hitl.md`. | `En cours` | 15.4 / **18.2** / **18.3** | OK | Nav label « Gestionnaire de Sessions » (`adminRoutes.js`) ; parité données liste = **K** jusqu’à OpenAPI. |
| `ui-admin-15-4-quick-analysis` | `page` | **`/admin/quick-analysis`** → `QuickAnalysis.tsx`. | **Gap CREOS**. | Analyses rapides — **15.2**. | `adminOnly`. | Résultats calculés côté serveur ou gap nommé. | **Dette** : page potentiellement peu utilisée — confirmer périmètre produit en **15.6**. | `A confirmer` | 15.4 | OK | Route absente de `ADMIN_NAVIGATION_ITEMS` — navigation secondaire ou lien interne ; pas d’exclusion silencieuse. |
| `ui-admin-15-4-cash-registers` | `page` | **`/admin/cash-registers`** → `CashRegisters.tsx`. | **Slice CREOS** story **17.2** : `page-transverse-admin-cash-registers.json` + nav servie + `admin.cash-registers.demo` (pas de `data_contract` ; gap **G-OA-02** / Registers absent OpenAPI canon au DS 2026-04-12). **17.3** : même coquille liste + slots `admin.transverse-list.*`. | Postes caisse — **15.2**. | `adminOnly` → CREOS `transverse.admin.view` + `requires_site` (**14.1**). | Liste / formulaire live : **bloqués** tant que l’OpenAPI canon ne porte pas les opérations — **Epic 16**. | Legacy `CashRegisters.test.tsx` + tests Peintre `navigation-transverse-served-5-1` / e2e `navigation-transverse-5-1` / **17.3** shell. | `En cours` | 15.4 / **17.2** / **17.3** | OK | Croiser **15.3** quand contrat publié. |
| `ui-admin-15-4-sites` | `page` | **`/admin/sites`** → `Sites.tsx`. | **Slice CREOS** story **17.2** : `page-transverse-admin-sites.json` + nav **`/admin/sites`** + `admin.sites.demo` ; hub distinct **`/admin/site`** = `page-transverse-admin-site-overview.json` (**14.2**) — relation explicitée `03-contrats-creos-et-donnees.md`. **17.3** : coquille + slots homogènes (gaps inchangés). | Sites / emplacements — **15.2**. | `adminOnly` → CREOS alignés hub admin. | CRUD live : **Epic 16** (gap Sites OpenAPI). | Matrice **14-02** + cartographie **15.2** + tests Peintre 17.2 / **17.3**. | `En cours` | 15.4 / 14.2 / **17.2** / **17.3** | OK | Pas de fusion silencieuse `/admin/site` ↔ `/admin/sites`. |
| `ui-admin-15-4-categories` | `page` | **`/admin/categories`** → `AdminCategories.tsx` (`ProtectedRoute` rôles ADMIN + SUPER_ADMIN). | **Gap CREOS**. | Catégories — **15.2**. | Rôle admin + super-admin. | Gestion catégories sans logique métier dupliquée dans Peintre. | **Dette** preuve + cartographie API. | `Backlog` | 15.4 | OK | — |
| `ui-admin-15-4-groups` | `page` | **`/admin/groups`** → `GroupsReal.tsx` (ADMIN + SUPER_ADMIN). | **Gap CREOS**. | Groupes / permissions — **15.2** ; surface **sensible**. | Rôles élevés. | Alignement modèle groupes v2 — **15.3** anti-patterns. | **Dette** runtime + audit code. | `Backlog` | 15.4 | OK | — |
| `ui-admin-15-4-audit-log` | `page` | **`/admin/audit-log`** → `AuditLog.tsx` (ADMIN + SUPER_ADMIN). | **Gap CREOS**. | Audit — **15.2**. | Rôles élevés. | Consultation audit traçable + exports éventuels. | **Dette** : journalisation à croiser OpenAPI. | `Backlog` | 15.4 | OK | Données sensibles — preuve navigateur sous compte autorisé uniquement. |
| `ui-admin-15-4-email-logs` | `page` | **`/admin/email-logs`** → `EmailLogs.tsx` (ADMIN + SUPER_ADMIN). | **Gap CREOS**. | Logs email — **15.2**. | Rôles élevés. | Liste / recherche / export selon backend. | **Dette**. | `Backlog` | 15.4 | OK | — |
| `ui-admin-15-4-health` | `page` | **`/admin/health`** → `HealthDashboard.tsx`. | **Gap CREOS** (monitoring hors lot admin transverse actuel). | Santé services — **15.2**. | `adminOnly`. | Indicateurs santé ou erreurs visibles cohérents API. | **Dette** : souvent environnement-dépendant ; pas de validation implicite « vert » sans preuve. | `Backlog` | 15.4 | OK | — |
| `ui-admin-15-4-settings` | `page` | **`/admin/settings`** → `Settings.tsx` (**SUPER_ADMIN** seul). | Voir ligne **14-02** (paramètres simples / hors duplication super-admin complète). | Paramètres globaux — **15.2** ; actions **step-up** possibles. | `SUPER_ADMIN`. | Parité bornée : pas de recréation métier ; slots démo vs legacy riche. | Matrice **14-02** + legacy. | `En cours` | 15.4 / 14.2 | OK | — |
| `ui-admin-15-4-sites-and-registers` | `page` | **`/admin/sites-and-registers`** → `SitesAndRegistersPage.tsx` (**SUPER_ADMIN**). | **Gap CREOS** page fusionnée. | Sites + registres — **15.2**. | `SUPER_ADMIN`. | Vue unifiée équivalente ou scission CREOS documentée. | **Dette** preuve. | `Backlog` | 15.4 | OK | Surface super-admin — aligner **15.3** (anti-pattern duplication). |
| `ui-admin-15-4-import-legacy` | `page` | **`/admin/import/legacy`** → `LegacyImport.tsx` (ADMIN + SUPER_ADMIN). | **Gap CREOS**. | Import — **15.2** ; opérations potentiellement **bulk** sensibles. | Rôles admin + super-admin. | Parité flux import ou **Hors scope** produit explicite si abandonné v2. | **Dette** : décision **15.5** / **15.6** si périmètre v2 exclut l’import legacy. | `A confirmer` | 15.4 / 15.6 | OK | Tant que non arbitré : statut *A confirmer*, pas *Valide*. |
