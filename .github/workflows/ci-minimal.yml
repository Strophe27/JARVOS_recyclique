# Baseline CI Epic 10 — Stories 10.1 + 10.2 + 10.3 + 10.4 + 10.5 (peloton + observabilité support)
# Gouvernance exécutable : chaque PR et chaque push sur `master` exécute API + Peintre + contrats (chaîne OpenAPI complète).
# Pas de filtre `paths:` sur ces jobs (AC2) — ne pas contourner avec continue-on-error ou scripts parallèles.
name: CI minimale (Recyclique · Peintre_nano · contrats)

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  api-minimal:
    name: API — compileall, ruff, pytest
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: recyclic_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version-file: recyclique/api/.python-version
          cache: pip
          cache-dependency-path: |
            recyclique/api/requirements.txt
            recyclique/api/requirements-dev.txt

      - name: Install dependencies
        working-directory: recyclique/api
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt
          pip install ruff

      - name: compileall
        working-directory: recyclique/api
        run: python -m compileall src/recyclic_api -q

      - name: ruff check
        working-directory: recyclique/api
        run: python -m ruff check src/recyclic_api

      - name: pytest critical core peloton
        working-directory: recyclique/api
        run: bash scripts/run_critical_core_peloton.sh
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/recyclic_test
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/recyclic_test
          REDIS_URL: redis://localhost:6379

      - name: pytest observability smokes (10.5)
        working-directory: recyclique/api
        run: |
          python -m pytest tests/test_story_10_5_http_correlation_peloton.py -q
          python -m pytest tests/test_story_10_5_sync_support_trail_smoke.py -q
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/recyclic_test
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/recyclic_test
          REDIS_URL: redis://localhost:6379

      - name: observability manifest guard (10.5)
        run: python -m pytest tests/infra/test_story_10_5_observability_manifest_guard.py -q

      - name: pytest (hors performance)
        working-directory: recyclique/api
        run: python -m pytest tests/ -m "not performance" --tb=short
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/recyclic_test
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/recyclic_test
          REDIS_URL: redis://localhost:6379

  peintre-nano-minimal:
    name: Peintre_nano — lint, gates 10.3, peloton 10.4, Vitest intégral
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: peintre-nano/package-lock.json

      - name: Install and lint
        working-directory: peintre-nano
        run: |
          npm ci
          npm run lint

      - name: Gate CREOS governance (10.3)
        working-directory: peintre-nano
        run: npx vitest run tests/contract/creos-manifests-governance-10-3.test.ts

      - name: Gate smoke rendu CREOS (10.3)
        working-directory: peintre-nano
        run: npx vitest run tests/smoke/creos-critical-render-paths-10-3.test.tsx

      - name: Peloton métier critical core (10.4)
        working-directory: peintre-nano
        run: npm run test:critical-core

      - name: Vitest intégral Peintre
        working-directory: peintre-nano
        run: npm run test

  contracts-openapi:
    name: Contrats — chaîne OpenAPI (FastAPI → snapshot → YAML → TS)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python (export FastAPI)
        uses: actions/setup-python@v5
        with:
          python-version-file: recyclique/api/.python-version
          cache: pip
          cache-dependency-path: |
            recyclique/api/requirements.txt
            recyclique/api/requirements-dev.txt

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: contracts/openapi/package-lock.json

      - name: Export contrats, codegen et working tree propre
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/recyclic_test
          REDIS_URL: redis://localhost:6379
          SECRET_KEY: ci-openapi-export
        run: |
          pip install --upgrade pip
          pip install -r recyclique/api/requirements.txt -r recyclique/api/requirements-dev.txt
          cd recyclique/api
          python generate_openapi.py --emit-contracts
          cd ../../contracts/openapi
          npm ci
          npm run generate
          git diff --exit-code generated/openapi-snapshot.json recyclique-api.yaml generated/recyclique-api.ts
